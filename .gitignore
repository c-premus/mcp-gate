# Binaries
/mcp-gate
*.exe
*.dll
*.so
*.dylib

# Build output
/dist/

# Test
*.test
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json

# Grafana dashboard project
grafana/node_modules/
grafana/dist/

# Environment (secrets)
.env
*.env.local

# Devcontainer CLI lock file — regenerated on every rebuild from the pinned
# image digests already tracked in .devcontainer/docker-compose.yaml, and it
# does not publish (the whole .devcontainer/ tree is scrubbed from the GitHub
# mirror). Tracking it would produce churn with no consumer.
.devcontainer/devcontainer-lock.json
